import React from 'react';
import ReactDOM, { render } from 'react-dom';
import { AppBar } from 'react-toolbox/lib/app_bar'

// const MenuBar = (props) => {
//   <App Bar {...props} />
// }

const MenuBar = AppBar

export default MenuBar
